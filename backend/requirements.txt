# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9      # required for file uploads (UploadFile)
pydantic-settings>=2.4

# --- ML / inference (same stack as the training notebook) ---
torch>=2.9
transformers>=4.30
paddleocr>=2.7,<3.0       # 2.x API: PaddleOCR(...).ocr(img, cls=True)
# Pin to 2.6.x: paddlepaddle 3.x crashes on Windows CPU with an oneDNN
# "fused_conv2d ... does not have the input Filter" error. Needs Python <=3.12.
paddlepaddle==2.6.2       # CPU build; swap for paddlepaddle-gpu to use CUDA
Pillow
numpy
